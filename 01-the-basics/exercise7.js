let foo = ['a', 'b', 'c'];
console.log(foo.length);  // => 3
console.log(foo[3]);  // This will be undefined because array indexing starts at 0, so valid indices are 0, 1, and 2. Index 3 is out of bounds.