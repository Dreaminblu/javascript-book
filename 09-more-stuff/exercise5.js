function doSomething(string) {
  return string.split('').reverse().map((value) => value.length);
}

// It splits the string into characters, reverses them, and returns an array of each character’s length.


